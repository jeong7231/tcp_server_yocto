BBPATH .= ":${LAYERDIR}"
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "jtylinux"
BBFILE_PATTERN_jtylinux = "^${LAYERDIR}/"
BBFILE_PRIORITY_jtylinux = "6"
LAYERSEIRIES_COMPAT_jtylinux = "kirkstone"
